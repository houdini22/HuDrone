#pragma once
#include "include.h"

/*
class MySerialPort : public QObject {
    Q_OBJECT
public:
    MySerialPort(QSerialPortInfo);
    bool open();
    bool connectDevice();
    bool isConnected();
    int write(QString);
private:
    QSerialPort * _device = nullptr;
    bool _is_connected = false;
private slots:
    void slotReadyRead();
    void slotBytesWritten(qint64);
};
*/
